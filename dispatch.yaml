dispatch:
  # Default module serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    service: default

  - url: "*/worker*"
    service: worker

  - url: "*/url2book*"
    service: worker

#  - url: "*/_ah/mail/*"
#    service: worker